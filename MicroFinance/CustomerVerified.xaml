<Page x:Class="MicroFinance.CustomerVerified"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MicroFinance"
      xmlns:convert="clr-namespace:MicroFinance.Converters"
      mc:Ignorable="d" 
      d:DesignHeight="630" d:DesignWidth="1366"
      Title="CustomerVerified" Background="#FEF2E4">
    <Page.Resources >
        <convert:VisibilityConverter x:Key="Visible"/>
        <convert:InverseVisible x:Key="Inverse"/>
        <convert:VisiblityConverterByImage x:Key="VisibleByImage"/>
        <convert:InverseVisiblityConverterbyImage x:Key="InverseVisibleImage"/>
        <convert:StatusForegroundColor x:Key="StatusColor"/>
        <convert:StatusImage x:Key="ImageConvert"/>
        <convert:GenderFemale x:Key="Femalecheck"/>
        <convert:GenderMaleCheck x:Key="MaleCheck"/>
        <convert:AgeConverter x:Key="AgeConvert"/>

    </Page.Resources>

    <Grid x:Name="PageGrid">
        <Grid.RowDefinitions >
            <RowDefinition Height="35"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <TextBlock Text="Verification of Customer" Style="{StaticResource Title}" FontWeight="Medium" x:Name="PAgeBlockText" />
        </StackPanel>
        <ScrollViewer Grid.Row="1" >
            <StackPanel x:Name="MainGrid">
                <Border Visibility="Visible" Width="700" Height="auto" BorderBrush="Black" BorderThickness="1" Background="#E6DDC6" CornerRadius="7">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius=".5"/>
                    </Border.Effect>
                    <StackPanel x:Name="customerNamegrid">
                        <Grid Grid.Row="1"  Margin="0,0,0,10">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Customer Details" Grid.Column="2" Style="{StaticResource SubTitle}" HorizontalAlignment="Center" Margin="0,10,0,10"/>
                            <Button x:Name="custDetailsVerifybtn" Grid.Column="4" Style="{StaticResource Verified}" Visibility="{Binding CustName,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,Converter={StaticResource Visible}}"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}" Visibility="{Binding CustDetailsOverAll,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,Converter={StaticResource Inverse}}" x:Name="custDetailsVerifiedbtn" Click="custDetailsVerifiedbtn_Click"/>
                        </Grid>
                        <Grid Grid.Row="1"  Margin="0,0,0,10">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="Customer Name"  Grid.Column="1" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="CustNameError"/>
                            </StackPanel>
                            <TextBox x:Name="CustNameBox"   Grid.Column="2" VerticalAlignment="Center" Style="{StaticResource Textbox}" Margin="0.4,0,0,0.4" >
                                <TextBox.Text>
                                    <Binding Path="CustomerName" Mode="TwoWay" UpdateSourceTrigger="PropertyChanged" />
                                </TextBox.Text>
                            </TextBox>

                            <Button x:Name="custNameVerfiedBtn" Grid.Column="4" Style="{StaticResource Verified}"  Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}" Visibility="Collapsed" Click="custNameVeriedBtn_Click" x:Name="custNameVeriedBtn"/>


                        </Grid>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="Gender" Grid.Row="1" Grid.Column="1" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="CustGenderError"/>
                            </StackPanel>
                            <RadioButton Content="Male" Margin="0,10,0,0" FontWeight="Medium" x:Name="CustMale"  Grid.Column="2" IsChecked="{Binding Gender,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,Converter={StaticResource MaleCheck }}">
                                <RadioButton.LayoutTransform>
                                    <ScaleTransform ScaleX="1.2" ScaleY="1.2"/>
                                </RadioButton.LayoutTransform>
                            </RadioButton>
                            <RadioButton Content="Female" Margin="85,10,10,0" FontWeight="Medium" x:Name="CustFemale" Grid.Column="2" IsChecked="{Binding Gender,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,Converter={StaticResource Femalecheck }}">
                                <RadioButton.LayoutTransform>
                                    <ScaleTransform ScaleX="1.2" ScaleY="1.2"/>
                                </RadioButton.LayoutTransform>
                            </RadioButton>

                            <Button Grid.Column="4" Style="{StaticResource Verified}" Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}" Visibility="Collapsed" x:Name="GenderVerification" Click="GenderVerification_Click"/>


                        </Grid>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="Date of Birth" Grid.Row="1" Grid.Column="1" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="CustDobError"/>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Grid.Column="2">
                                <DatePicker Grid.Column="2" x:Name="CustDOB" Width="120" Height="30" VerticalContentAlignment="Center" HorizontalAlignment="Left" Margin="15,0,20,0" SelectedDate="{Binding DateofBirth,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"/>
                                <TextBlock Text="Age" Style="{StaticResource Regular}"/>
                                <TextBlock Text="{Binding  DateofBirth,Mode=OneWay,UpdateSourceTrigger=PropertyChanged,Converter={StaticResource AgeConvert}}"  Margin="20,5,0,0"/>

                            </StackPanel>
                            <Button Grid.Column="4" Style="{StaticResource Verified}" Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}" Visibility="Collapsed" x:Name="CustDobVeification" Click="CustDobVeification_Click"/>


                        </Grid>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="Father Name" Grid.Row="1" Grid.Column="1" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="CustFatherError"/>
                            </StackPanel>
                            <TextBox Style="{StaticResource Textbox}" Grid.Column="2" Text="{Binding FatherName,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"  x:Name="CustFatherName">
                            </TextBox>

                            <Button Grid.Column="4" Style="{StaticResource Verified}" Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}" Visibility="Collapsed" x:Name="CustFatherVerification" Click="CustFatherVerification_Click"/>


                        </Grid>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                
                                <StackPanel Orientation="Horizontal" Grid.Column="1">
                                    <TextBlock Text="Mother Name" Grid.Row="1"  Style="{StaticResource Regular}"/>
                                    <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="CustMotherError"/>
                                </StackPanel>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed"/>
                            </StackPanel>
                            <TextBox Style="{StaticResource Textbox}" Grid.Column="2" Text="{Binding MotherName,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" x:Name="CustMotherName">
                            </TextBox>

                            <Button Grid.Column="4" Style="{StaticResource Verified}" Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}" Visibility="Collapsed" x:Name="CustMotherVerification" Click="CustMotherVerification_Click"/>


                        </Grid>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="185"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="Husband/Guardian Name" Grid.Row="1"  Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="CustHusbandError"/>
                            </StackPanel>
                            <TextBox Style="{StaticResource Textbox}" Grid.Column="2" Text="{Binding HusbandName,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" x:Name="CustHusbandName">
                            </TextBox>

                            <Button Grid.Column="4" Style="{StaticResource Verified}" Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}" Visibility="Collapsed" x:Name="CustHusbandVerification" Click="CustHusbandVerification_Click"/>


                        </Grid>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="Contact Number" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="CustContactError"/>
                            </StackPanel>
                            <TextBox Style="{StaticResource Textbox}" Grid.Column="2" Text="{Binding ContactNumber,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" x:Name="CustContactNo">
                            </TextBox>

                            <Button Grid.Column="4" Style="{StaticResource Verified}" Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}" Visibility="Collapsed" x:Name="CustContactVerification" Click="CustContactVerification_Click"/>


                        </Grid>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="Aadhar Number" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="CustAadharError"/>
                            </StackPanel>
                            <TextBox Style="{StaticResource Textbox}" Grid.Column="2" Text="{Binding AadharNo,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" x:Name="CustAadharNo">
                            </TextBox>

                            <Button Grid.Column="4" Style="{StaticResource Verified}" Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}" Visibility="Collapsed"  x:Name="CustAadharVerification" Click="CustAadharVerification_Click"/>
                </Grid>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="Religion" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="CustReligionError"/>
                            </StackPanel>
                            <ComboBox Grid.Column="2" x:Name="CustReligion"  VerticalAlignment="Center" Width="140" Style="{StaticResource combo}" HorizontalAlignment="Left"  Text="{Binding Religion,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" >
                                <ComboBoxItem Content="HINDU"/>
                                <ComboBoxItem Content="MUSLIM"/>
                                <ComboBoxItem Content="CHRISTIAN"/>
                            </ComboBox>

                            <Button Grid.Column="4" Style="{StaticResource Verified}" Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}" Visibility="Collapsed"/>


                        </Grid>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="Community" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="CustCommunityError"/>
                            </StackPanel>
                            <ComboBox x:Name="CommunityBox" VerticalAlignment="Center" Width="140" Style="{StaticResource combo}" HorizontalAlignment="Left" Text="{Binding Community,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" IsEnabled="False" Margin="0,0,0,0" SelectionChanged="CommunityBox_SelectionChanged" Grid.Column="2">
                                <ComboBoxItem Content="SC"/>
                                <ComboBoxItem Content="ST"/>
                                <ComboBoxItem Content="MBC"/>
                                <ComboBoxItem Content="DC"/>
                                <ComboBoxItem Content="BC"/>
                                <ComboBoxItem Content="BCM"/>
                            </ComboBox>

                            <Button Grid.Column="4" Style="{StaticResource Verified}" Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}" Visibility="Collapsed" x:Name="CustCommunityVerification" Click="CustCommunityVerification_Click"/>


                        </Grid>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="Caste" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="CustCasteError"/>
                            </StackPanel>
                            <TextBox Style="{StaticResource Textbox}" Grid.Column="2" Text="{Binding Caste,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" x:Name="CustCaste"/>


                            <Button Grid.Column="4" Style="{StaticResource Verified}" Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}" Visibility="Collapsed" x:Name="CustCasteVerification" Click="CustCasteVerification_Click"/>


                        </Grid>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="Education" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="CustEducationError"/>
                            </StackPanel>
                            <TextBox Style="{StaticResource Textbox}" Grid.Column="2" Text="{Binding Education,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" x:Name="CustEducation"/>
                            <Button Grid.Column="4" Style="{StaticResource Verified}" Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}" Visibility="Collapsed" Click="CustEducationVerification_Click" x:Name="CustEducationVerification"/>


                        </Grid>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="Family Members" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="CustFamilyMembersError"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Grid.Column="2">
                                <TextBox Style="{StaticResource Textbox}" Grid.Column="2" Width="50" HorizontalAlignment="Left" Text="{Binding FamilyMembers,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" x:Name="CustFamilyMembers">
                                </TextBox>
                                <TextBlock Text="Earning Members" Style="{StaticResource Regular}" Margin="20,0,0,0"/>
                                <TextBox Style="{StaticResource Textbox}" Grid.Column="2" Width="50" HorizontalAlignment="Right" Text="{Binding EarningMembers,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Margin="30,0,0,0"/>
                            </StackPanel>

                            <Button Grid.Column="4" Style="{StaticResource Verified}" Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}" Visibility="Collapsed" x:Name="CustFamilyMemberVerification" Click="CustFamilyMemberVerification_Click"/>


                        </Grid>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="Occupation" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="CustOccupationError"/>
                            </StackPanel>
                            <TextBox Style="{StaticResource Textbox}" Grid.Column="2" Text="{Binding Occupation,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" x:Name="CustOccupation"/>


                            <Button Grid.Column="4" Style="{StaticResource Verified}" Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}" Visibility="Collapsed"  x:Name="CustOccupationVerification" Click="CustOccupationVerification_Click"/>


                        </Grid>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="140"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="Monthly Income" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="CustmontlyIncomeError"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Grid.Column="2">
                                <TextBox Style="{StaticResource Textbox}" Grid.Column="2" Width="80" HorizontalAlignment="Left" Text="{Binding MonthlyIncome,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" x:Name="CustMonthlyIncome">
                                </TextBox>
                                <TextBlock Text="Monthly Expenses" Style="{StaticResource Regular}" Margin="10,0,0,0"/>
                                <TextBox Style="{StaticResource Textbox}" Grid.Column="2" Width="80" HorizontalAlignment="Right" Text="{Binding MothlyExpenses,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Margin="15,0,0,0" x:Name="CustMonthlyExpense"/>
                            </StackPanel>

                            <Button Grid.Column="4" Style="{StaticResource Verified}" Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}" Visibility="Collapsed" x:Name="CustMonthlyVerification" Click="CustMonthlyVerification_Click"/>


                        </Grid>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="Family Yearly Income" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="CustYearlyIncomeError"/>
                            </StackPanel>
                            <TextBox Style="{StaticResource Textbox}" Grid.Column="2" Text="{Binding YearlyIncome,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" x:Name="CustYearlyIncome">
                            </TextBox>

                            <Button Grid.Column="4" Style="{StaticResource Verified}" Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}" Visibility="Collapsed" x:Name="CustYearlyVerification" Click="CustYearlyVerification_Click"/>


                        </Grid>
                    </StackPanel>
                </Border>

                <Border Visibility="Visible" Width="700" Height="auto" BorderBrush="Black" BorderThickness="1" Margin="0,15,0,0" Background="#E6DDC6" CornerRadius="7">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius=".5"/>
                    </Border.Effect>
                    <StackPanel x:Name="customerAddressGrid">
                       <Grid Grid.Row="1"  Margin="0,0,0,10">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Customer Address Details" Grid.Column="2" Style="{StaticResource SubTitle}" HorizontalAlignment="Center" Margin="0,10,0,10"/>
                            <Button x:Name="custaddressVerifybtn" Grid.Column="4" Style="{StaticResource Verified}" Visibility="{Binding CustAddressOverAll,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,Converter={StaticResource Visible}}"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}" Visibility="{Binding CustAddressOverAll,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,Converter={StaticResource Inverse}}" x:Name="custAddressVerifiedbtn" Click="custAddressVerifiedbtn_Click"/>
                        </Grid>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="Door Number" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="CustDoorNoError"/>
                            </StackPanel>
                            <TextBox Style="{StaticResource Textbox}" Grid.Column="2" Text="{Binding DoorNumber,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" x:Name="CustDoorNo">
                            </TextBox>

                            <Button Grid.Column="4" Style="{StaticResource Verified}" Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}" Visibility="Collapsed" x:Name="CustDoorNoVerification" Click="CustDoorNoVerification_Click"/>


                        </Grid>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="Street Name" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="CustStreetNameError"/>
                            </StackPanel>
                            <TextBox Style="{StaticResource Textbox}" Grid.Column="2" Text="{Binding StreetName,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" x:Name="CustStreetName">
                            </TextBox>

                            <Button Grid.Column="4" Style="{StaticResource Verified}" Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}" Visibility="Collapsed" x:Name="CustStreetVerification" Click="CustStreetVerification_Click"/>


                        </Grid>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="Locality/Town" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="CustLocalityError"/>
                            </StackPanel>
                            <TextBox Style="{StaticResource Textbox}" Grid.Column="2" Text="{Binding LocalityTown,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" x:Name="CustLocality">
                            </TextBox>

                            <Button Grid.Column="4" Style="{StaticResource Verified}" Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}" Visibility="Collapsed" x:Name="CustLocalityVerification" Click="CustLocalityVerification_Click"/>


                        </Grid>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="City" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="CustCityError"/>
                            </StackPanel>
                            <TextBox Style="{StaticResource Textbox}" Grid.Column="2" Text="{Binding City,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" x:Name="CustCity">
                            </TextBox>

                            <Button Grid.Column="4" Style="{StaticResource Verified}" Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}" Visibility="Collapsed" x:Name="CustCityVerification" Click="CustCityVerification_Click"/>


                        </Grid>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="State" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="CustStateError"/>
                            </StackPanel>
                            <TextBox Style="{StaticResource Textbox}" Grid.Column="2" Text="{Binding State,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" x:Name="CustState">
                            </TextBox>

                            <Button Grid.Column="4" Style="{StaticResource Verified}" Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}" Visibility="Collapsed" x:Name="CustStateVerification" Click="CustStateVerification_Click"/>


                        </Grid>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="Pincode" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="CustPincodeError"/>
                            </StackPanel>
                            <TextBox Style="{StaticResource Textbox}" Grid.Column="2" Text="{Binding Pincode,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" x:Name="CustPincode">
                            </TextBox>

                            <Button Grid.Column="4" Style="{StaticResource Verified}" Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}" Visibility="Collapsed" x:Name="CustPincodeVerification" Click="CustPincodeVerification_Click"/>


                        </Grid>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="Housing Type" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="CustHousingTypeError"/>
                            </StackPanel>
                            <TextBox Style="{StaticResource Textbox}" Grid.Column="2" Text="{Binding HousingType,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" x:Name="CustHousingType">
                            </TextBox>

                            <Button Grid.Column="4" Style="{StaticResource Verified}" Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}" Visibility="Collapsed" x:Name="CustHousingTypeVerification" Click="CustHousingTypeVerification_Click"/>


                        </Grid>
                    </StackPanel>
                </Border>

                <Border Visibility="Visible" Width="700" Height="auto" BorderBrush="Black" BorderThickness="1" Margin="0,15,0,0" Background="#E6DDC6" CornerRadius="7">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius=".5"/>
                    </Border.Effect>
                    <StackPanel x:Name="Guarantorgrid">
                        <Grid Grid.Row="1"  Margin="0,0,0,10">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Guarantor Details" Grid.Column="2" Style="{StaticResource SubTitle}" HorizontalAlignment="Center" Margin="0,10,0,10"/>
                            <Button x:Name="GuarantorOverAllVerifybtn" Grid.Column="4" Style="{StaticResource Verified}" Visibility="{Binding OverAllBasicDetailsofGuarantor,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,Converter={StaticResource Visible}}"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}" Visibility="{Binding OverAllBasicDetailsofGuarantor,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,Converter={StaticResource Inverse}}" x:Name="GuarantorOverAllVerifiedbtn" Click="GuarantorOverAllVerifiedbtn_Click"/>
                        </Grid>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="Name" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="GNameError"/>
                            </StackPanel>
                            <TextBox Style="{StaticResource Textbox}" Grid.Column="2" Text="{Binding GuarantorName,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" x:Name="GName">
                            </TextBox>

                            <Button Grid.Column="4" Style="{StaticResource Verified}" Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}"  Visibility="Collapsed" Click="GNameVerification_Click"/>


                        </Grid>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="Gender" Grid.Row="1" Grid.Column="1" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="GGenderError"/>
                            </StackPanel>
                            <RadioButton Content="Male" x:Name="GMALE" Margin="0,10,0,0" FontWeight="Medium"  Grid.Column="2" IsChecked="{Binding Gender,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,Converter={StaticResource MaleCheck }}">
                                <RadioButton.LayoutTransform>
                                    <ScaleTransform ScaleX="1.2" ScaleY="1.2"/>
                                </RadioButton.LayoutTransform>
                            </RadioButton>
                            <RadioButton Content="Female" x:Name="GFEMALE" Margin="85,10,10,0" FontWeight="Medium" Grid.Column="2" IsChecked="{Binding Gender,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,Converter={StaticResource Femalecheck }}">
                                <RadioButton.LayoutTransform>
                                    <ScaleTransform ScaleX="1.2" ScaleY="1.2"/>
                                </RadioButton.LayoutTransform>
                            </RadioButton>

                            <Button Grid.Column="4" Style="{StaticResource Verified}"  Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}"  Visibility="Collapsed" x:Name="GGenderVerification" Click="GGenderVerification_Click"/>


                        </Grid>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="Date of Birth" Grid.Row="1" Grid.Column="1" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="GDObError"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Grid.Column="2">
                                <DatePicker Grid.Column="2" x:Name="GDobSelect" Width="120" Height="30" VerticalContentAlignment="Center" HorizontalAlignment="Left" Margin="15,0,20,0" SelectedDate="{Binding DateofBirth,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"/>
                                <TextBlock Text="Age" Style="{StaticResource Regular}"/>
                                <TextBlock Text="{Binding  DateofBirth,Mode=OneWay,UpdateSourceTrigger=PropertyChanged,Converter={StaticResource AgeConvert}}"  Margin="20,5,0,0"/>

                            </StackPanel>
                            <Button Grid.Column="4" Style="{StaticResource Verified}"  Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}"  Visibility="Collapsed" x:Name="GDobVerification" Click="GDobVerification_Click"/>


                        </Grid>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="Contact Number" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="GContactError"/>
                            </StackPanel>
                            <TextBox Style="{StaticResource Textbox}" Grid.Column="2" Text="{Binding ContactNumber,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" x:Name="GContact">
                            </TextBox>

                            <Button Grid.Column="4" Style="{StaticResource Verified}"  Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}"  Visibility="Collapsed" x:Name="GContactVerification" Click="GContactVerification_Click"/>


                        </Grid>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="Occupation" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="GOccupationError"/>
                            </StackPanel>
                            <TextBox Style="{StaticResource Textbox}" Grid.Column="2" Text="{Binding Occupation,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" x:Name="GOccupation">
                            </TextBox>

                            <Button Grid.Column="4" Style="{StaticResource Verified}" Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}"  Visibility="Collapsed" x:Name="GOccupationVerification" Click="GOccupationVerification_Click"/>


                        </Grid>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="Relationship" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="GRelationshipError"/>
                            </StackPanel>
                            <ComboBox Grid.Column="2"  VerticalAlignment="Center" Width="140" Style="{StaticResource combo}" HorizontalAlignment="Left" x:Name="Gselectreligion"  Text="{Binding RelationShip,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" >
                                <ComboBoxItem Content="HUSBAND"/>
                                <ComboBoxItem Content="FATHER"/>
                                <ComboBoxItem Content="MOTHER"/>
                                <ComboBoxItem Content="SISTER"/>
                                <ComboBoxItem Content="BROTHER"/>
                                <ComboBoxItem Content="SON"/>
                                <ComboBoxItem Content="DAUGHTER"/>
                            </ComboBox>

                            <Button Grid.Column="4" Style="{StaticResource Verified}"  Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}"  Visibility="Collapsed" x:Name="GRelationshipVerification" Click="GRelationshipVerification_Click"/>


                        </Grid>

                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="Door Number" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="GDoorNoError"/>
                            </StackPanel>
                            <TextBox Style="{StaticResource Textbox}" Grid.Column="2" Text="{Binding DoorNumber,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" x:Name="GDoorNo">
                            </TextBox>

                            <Button Grid.Column="4" Style="{StaticResource Verified}" Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}" Visibility="Collapsed" x:Name="GDoorNoVerification" Click="GDoorNoVerification_Click"/>


                        </Grid>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="Street Name" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="GStreetNameError"/>
                            </StackPanel>
                            <TextBox Style="{StaticResource Textbox}" Grid.Column="2" Text="{Binding StreetName,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" x:Name="GStreetName">
                            </TextBox>

                            <Button Grid.Column="4" Style="{StaticResource Verified}"  Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}"  Visibility="Collapsed" x:Name="GStreetVerification" Click="GStreetVerification_Click"/>


                        </Grid>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="Locality/Town" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="GLocalityError"/>
                            </StackPanel>
                            <TextBox Style="{StaticResource Textbox}" Grid.Column="2" Text="{Binding LocalityTown,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" x:Name="GLocality">
                            </TextBox>

                            <Button Grid.Column="4" Style="{StaticResource Verified}"  Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}"  Visibility="Collapsed" x:Name="GLocalityVerification" Click="GLocalityVerification_Click"/>


                        </Grid>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="City" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="GCityError"/>
                            </StackPanel>
                            <TextBox Style="{StaticResource Textbox}" Grid.Column="2" Text="{Binding City,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" x:Name="GCity">
                            </TextBox>

                            <Button Grid.Column="4" Style="{StaticResource Verified}"  Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}"  Visibility="Collapsed" x:Name="GCityVerification" Click="GCityVerification_Click"/>


                        </Grid>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="State" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="GStateError"/>
                            </StackPanel>
                            <TextBox Style="{StaticResource Textbox}" Grid.Column="2" Text="{Binding State,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" x:Name="GState">
                            </TextBox>

                            <Button Grid.Column="4" Style="{StaticResource Verified}"  Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}"  Visibility="Collapsed" x:Name="GStateVerification" Click="GStateVerification_Click"/>


                        </Grid>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="Pincode" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="GPincodeError"/>
                            </StackPanel>
                            <TextBox Style="{StaticResource Textbox}" Grid.Column="2" Text="{Binding Pincode,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" x:Name="GPincode">
                            </TextBox>

                            <Button Grid.Column="4" Style="{StaticResource Verified}"  Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}"  Visibility="Collapsed" x:Name="GPincodeVerification" Click="GPincodeVerification_Click"/>
                        </Grid>
                    </StackPanel>
                </Border>
                <Border Visibility="Visible" Width="700" Height="auto" BorderBrush="Black" BorderThickness="1" Margin="0,15,0,0" Background="#E6DDC6" CornerRadius="7">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius=".5"/>
                    </Border.Effect>
                    <StackPanel x:Name="NomineeGrid">
                        <Grid Grid.Row="1"  Margin="0,0,0,10">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Nominee Details" Grid.Column="2" Style="{StaticResource SubTitle}" HorizontalAlignment="Center" Margin="0,10,0,10"/>
                            <Button x:Name="NomineeOverAllVerifybtn" Grid.Column="4" Style="{StaticResource Verified}" Visibility="{Binding OverAllBasicDetailsofNominee,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,Converter={StaticResource Visible}}"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}" Visibility="{Binding OverAllBasicDetailsofNominee,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,Converter={StaticResource Inverse}}" x:Name="NomineeOverAllVerifiedbtn" Click="NomineeOverAllVerifiedbtn_Click"/>
                        </Grid>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="Name" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="NNameError"/>
                            </StackPanel>
                            <TextBox Style="{StaticResource Textbox}" Grid.Column="2" Text="{Binding NomineeName,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" x:Name="NName">
                            </TextBox>

                            <Button Grid.Column="4" Style="{StaticResource Verified}" Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}"  Visibility="Collapsed" x:Name="NNameVerification" Click="NNameVerification_Click"/>


                        </Grid>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="Gender" Grid.Row="1" Grid.Column="1" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="NGenderError"/>
                            </StackPanel>
                            <RadioButton Content="Male" x:Name="NMALE" Margin="0,10,0,0" FontWeight="Medium"  Grid.Column="2" IsChecked="{Binding Gender,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,Converter={StaticResource MaleCheck }}">
                                <RadioButton.LayoutTransform>
                                    <ScaleTransform ScaleX="1.2" ScaleY="1.2"/>
                                </RadioButton.LayoutTransform>
                            </RadioButton>
                            <RadioButton Content="Female" x:Name="NFEMALE" Margin="85,10,10,0" FontWeight="Medium" Grid.Column="2" IsChecked="{Binding Gender,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,Converter={StaticResource Femalecheck }}">
                                <RadioButton.LayoutTransform>
                                    <ScaleTransform ScaleX="1.2" ScaleY="1.2"/>
                                </RadioButton.LayoutTransform>
                            </RadioButton>

                            <Button Grid.Column="4" Style="{StaticResource Verified}"  Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}"  Visibility="Collapsed" x:Name="NGenderVerirication" Click="NGenderVerirication_Click"/>


                        </Grid>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="Date of Birth" Grid.Row="1" Grid.Column="1" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="NDObError"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Grid.Column="2">
                                <DatePicker Grid.Column="2" Width="120" Height="30" VerticalContentAlignment="Center" HorizontalAlignment="Left" Margin="15,0,20,0" x:Name="NDobSelect" SelectedDate="{Binding DateofBirth,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"/>
                                <TextBlock Text="Age" Style="{StaticResource Regular}"/>
                                <TextBlock Text="{Binding  DateofBirth,Mode=OneWay,UpdateSourceTrigger=PropertyChanged,Converter={StaticResource AgeConvert}}" Style="{StaticResource NumberRegular}" FontWeight="Medium" Margin="20,0,0,5"/>

                            </StackPanel>
                            <Button Grid.Column="4" Style="{StaticResource Verified}"  Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}"  Visibility="Collapsed" x:Name="NDobVerification" Click="NDobVerification_Click"/>


                        </Grid>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="Contact Number" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="NContactError"/>
                            </StackPanel>
                            <TextBox Style="{StaticResource Textbox}" Grid.Column="2" Text="{Binding ContactNumber,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" x:Name="NContact">
                            </TextBox>

                            <Button Grid.Column="4" Style="{StaticResource Verified}"  Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}" Visibility="Collapsed" x:Name="NcontactVerification" Click="NcontactVerification_Click"/>


                        </Grid>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="Occupation" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="NOccupationError"/>
                            </StackPanel>
                            <TextBox Style="{StaticResource Textbox}" Grid.Column="2" Text="{Binding Occupation,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" x:Name="NOccupation">
                            </TextBox>

                            <Button Grid.Column="4" Style="{StaticResource Verified}"  Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}"   Visibility="Collapsed" x:Name="NOccupationVerification" Click="NOccupationVerification_Click"/>


                        </Grid>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="Relationship" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="NRelationshipError"/>
                            </StackPanel>
                            <ComboBox Grid.Column="2"  VerticalAlignment="Center" Width="140" Style="{StaticResource combo}" HorizontalAlignment="Left" x:Name="NSELECTRELIGION"  Text="{Binding RelationShip,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" >
                                <ComboBoxItem Content="HUSBAND"/>
                                <ComboBoxItem Content="FATHER"/>
                                <ComboBoxItem Content="MOTHER"/>
                                <ComboBoxItem Content="SISTER"/>
                                <ComboBoxItem Content="BROTHER"/>
                                <ComboBoxItem Content="SON"/>
                                <ComboBoxItem Content="DAUGHTER"/>
                            </ComboBox>

                            <Button Grid.Column="4" Style="{StaticResource Verified}"  Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}"   Visibility="Collapsed" x:Name="NRelationVerification" Click="NRelationVerification_Click"/>
 

                        </Grid>

                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="Door Number" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="NDoorNoError"/>
                            </StackPanel>
                            <TextBox Style="{StaticResource Textbox}" Grid.Column="2" Text="{Binding DoorNumber,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" x:Name="NDoorNo">
                            </TextBox>

                            <Button Grid.Column="4" Style="{StaticResource Verified}"   Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}"   Visibility="Collapsed" x:Name="NDoorVerification" Click="NDoorVerification_Click"/>


                        </Grid>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="Street Name" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="NStreetNameError"/>
                            </StackPanel>
                            <TextBox Style="{StaticResource Textbox}" Grid.Column="2" Text="{Binding StreetName,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" x:Name="NStreetName">
                            </TextBox>

                            <Button Grid.Column="4" Style="{StaticResource Verified}"   Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}"   Visibility="Collapsed" x:Name="NStreetVerification" Click="NStreetVerification_Click"/>


                        </Grid>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="Locality/Town" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="NLocalityError"/>
                            </StackPanel>
                            <TextBox Style="{StaticResource Textbox}" Grid.Column="2" Text="{Binding LocalityTown,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" x:Name="NLocality">
                            </TextBox>

                            <Button Grid.Column="4" Style="{StaticResource Verified}"   Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}"   Visibility="Collapsed" x:Name="NLocalityVerification" Click="NLocalityVerification_Click"/>


                        </Grid>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="City" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="NCityError"/>
                            </StackPanel>
                            <TextBox Style="{StaticResource Textbox}" Grid.Column="2" Text="{Binding City,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" x:Name="NCity">
                            </TextBox>

                            <Button Grid.Column="4" Style="{StaticResource Verified}"   Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}"   Visibility="Collapsed" x:Name="NCityVErification" Click="NCityVErification_Click"/>


                        </Grid>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="State" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="NStateError"/>
                            </StackPanel>
                            <TextBox Style="{StaticResource Textbox}" Grid.Column="2" Text="{Binding State,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" x:Name="NState">
                            </TextBox>

                            <Button Grid.Column="4" Style="{StaticResource Verified}"   Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}"   Visibility="Collapsed" x:Name="NStateVerification" Click="NStateVerification_Click"/>
                        </Grid>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="Pincode" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="NPincodeError"/>
                            </StackPanel>
                            <TextBox Style="{StaticResource Textbox}" Grid.Column="2" Text="{Binding Pincode,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" x:Name="NPincode">
                            </TextBox>

                            <Button Grid.Column="4" Style="{StaticResource Verified}"   Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}"  Visibility="Collapsed" x:Name="NPincodeVerification" Click="NPincodeVerification_Click"/>


                        </Grid>
                    </StackPanel>
                </Border>

                <Border Visibility="Visible" Width="700" Height="auto" BorderBrush="Black" BorderThickness="1" Margin="0,15,0,0" Background="#E6DDC6" CornerRadius="7">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius=".5"/>
                    </Border.Effect>
                    <StackPanel x:Name="BankDetailsGrid">
                        <Grid Grid.Row="1"  Margin="0,0,0,10">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Bank Details" Grid.Column="2" Style="{StaticResource SubTitle}" HorizontalAlignment="Center" Margin="0,10,0,10"/>
                            <Button x:Name="BankOverAllVerifybtn" Grid.Column="4" Style="{StaticResource Verified}" Visibility="{Binding CustBankDetailsOverAll,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,Converter={StaticResource Visible}}"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}" Visibility="{Binding CustBankDetailsOverAll,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,Converter={StaticResource Inverse}}" x:Name="BankOverAllVerifiedbtn" Click="BankOverAllVerifiedbtn_Click"/>
                        </Grid>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="Account Holder Name" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="AccountHolderError"/>
                            </StackPanel>
                            <TextBox Style="{StaticResource Textbox}" Grid.Column="2" Text="{Binding AccountHolder,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" x:Name="AccountHolder">
                            </TextBox>

                            <Button Grid.Column="4" Style="{StaticResource Verified}" Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}" Visibility="Collapsed" x:Name="HolderVerification" Click="HolderVerification_Click"/>


                        </Grid>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="Account Number" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="AccountNoError"/>
                            </StackPanel>
                            <TextBox Style="{StaticResource Textbox}" Grid.Column="2" Text="{Binding AccountNumber,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" x:Name="AccountNo">
                            </TextBox>

                            <Button Grid.Column="4" Style="{StaticResource Verified}" Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}" Visibility="Collapsed" x:Name="AccountNoVerification" Click="AccountNoVerification_Click"/>


                        </Grid>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="Bank Name" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="BAnkNameError"/>
                            </StackPanel>
                            <TextBox Style="{StaticResource Textbox}" Grid.Column="2" Text="{Binding BankName,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" x:Name="BAnkName">
                            </TextBox>

                            <Button Grid.Column="4" Style="{StaticResource Verified}" Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}" Visibility="Collapsed" x:Name="BankNameVerification" Click="BankNameVerification_Click"/>


                        </Grid>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="Branch Name" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="BAnkBranchError"/>
                            </StackPanel>
                            <TextBox Style="{StaticResource Textbox}" Grid.Column="2" Text="{Binding BankBranchName
,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" x:Name="BAnkBranch">
                            </TextBox>

                            <Button Grid.Column="4" Style="{StaticResource Verified}" Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}" Visibility="Collapsed" x:Name="BranchNameVerification" Click="BranchNameVerification_Click"/>


                        </Grid>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="IFSC Code" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="IFSCError"/>
                            </StackPanel>
                            <TextBox Style="{StaticResource Textbox}" Grid.Column="2" Text="{Binding IFSCCode,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"  x:Name="IFSC">
                            </TextBox>

                            <Button Grid.Column="4" Style="{StaticResource Verified}" Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}" Visibility="Collapsed" x:Name="IFscVerification" Click="IFscVerification_Click"/>


                        </Grid>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="MICR Code" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="MICRError"/>
                            </StackPanel>
                            <TextBox Style="{StaticResource Textbox}" Grid.Column="2" Text="{Binding MICRCode,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" x:Name="MICR">
                            </TextBox>

                            <Button Grid.Column="4" Style="{StaticResource Verified}" Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}" Visibility="Collapsed" x:Name="MicrVerification" Click="MicrVerification_Click"/>


                        </Grid>
                    </StackPanel>
                </Border>
                <Border Visibility="Visible" Width="700" Height="auto" BorderBrush="Black" BorderThickness="1" Margin="0,15,0,0" Background="#E6DDC6" CornerRadius="7" x:Name="DocumentPanel">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius=".5"/>
                    </Border.Effect>
                    <StackPanel x:Name="CustDocumentGrid">
                        <Grid Grid.Row="1"  Margin="0,0,0,10">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Document Details" Grid.Column="2" Style="{StaticResource SubTitle}" HorizontalAlignment="Center" Margin="0,10,0,10"/>

                            <Button x:Name="OverAllCustDocumentVerify" Grid.Column="4" Style="{StaticResource Verified}" Visibility="{Binding OverAllPhotoVerification,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,Converter={StaticResource Visible}}"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}" Visibility="{Binding OverAllPhotoVerification,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,Converter={StaticResource Inverse}}" x:Name="OverAllCustDocumentVerifiedBtn" Click="OverAllDocumentVerifiedBtn_Click"/>

                        </Grid>
                        <Grid Margin="0,0,0,10"  x:Name="customerAddressProofgrid">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1" >
                                <TextBlock Text="Customer Address Proof" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="CustAddressProofError"/>
                            </StackPanel>
                            <ComboBox Grid.Column="2"  VerticalAlignment="Center" Width="200" Style="{StaticResource combo}" HorizontalAlignment="Left" ItemsSource="{Binding AddressProofName,Mode=OneWay,UpdateSourceTrigger=PropertyChanged}" Text="{Binding NameofAddressProof,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" x:Name="CustNameofAddresProof" SelectionChanged="CustNameofAddresProof_SelectionChanged">

                            </ComboBox>
                            <Grid Grid.Column="3" >
                                <Button  Style="{StaticResource Add}" Visibility="{Binding AddressProof,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,Converter={StaticResource InverseVisibleImage}}" x:Name="AddCustAddressProof" Click="AddCustAddressProof_Click" IsEnabled="False"/>
                                <StackPanel Orientation="Horizontal" Grid.Column="3" Visibility="{Binding AddressProof,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,Converter={StaticResource VisibleByImage}}">
                                    <Button  Style="{StaticResource Show}" x:Name="ShowCustAddressProof" Click="ShowCustAddressProof_Click"/>
                                    <Button Grid.Column="3" Style="{StaticResource Edit}" x:Name="EditCustAddressProof" Click="EditCustAddressProof_Click"/>
                                </StackPanel>
                            </Grid>
                            <Button Grid.Column="4" Style="{StaticResource Verified}" Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}" Visibility="Collapsed" x:Name="CustAddressProofVerification" Click="CustAddressProofVerification_Click"/>


                        </Grid>
                        <Grid Margin="0,0,0,10"  x:Name="customerPhotoProofGrid">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="Customer Photo Proof" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="CustPhotoProofError"/>
                            </StackPanel>
                            <ComboBox Grid.Column="2"  VerticalAlignment="Center" Width="200" Style="{StaticResource combo}" HorizontalAlignment="Left" ItemsSource="{Binding AddressProofName,Mode=OneWay,UpdateSourceTrigger=PropertyChanged}" Text="{Binding NameofPhotoProof,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" x:Name="CustNameofPhotoProof" SelectionChanged="CustNameofPhotoProof_SelectionChanged">

                            </ComboBox>
                            <Grid Grid.Column="3">
                                <Button Grid.Column="3" Style="{StaticResource Add}" Visibility="{Binding PhotoProof,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,Converter={StaticResource InverseVisibleImage}}" x:Name="CustPhotoProofAdd" Click="CustPhotoProofAdd_Click" IsEnabled="False"/>
                                <StackPanel Orientation="Horizontal" Grid.Column="3" Visibility="{Binding PhotoProof,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,Converter={StaticResource VisibleByImage}}">
                                    <Button  Style="{StaticResource Show}" x:Name="CustPhotoProofView" Click="CustPhotoProofView_Click"/>
                                    <Button Grid.Column="3" Style="{StaticResource Edit}" x:Name="CustPhotoProofEdit" Click="CustPhotoProofEdit_Click"/>
                                </StackPanel>
                            </Grid>
                            <Button Grid.Column="4" Style="{StaticResource Verified}" Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}" Visibility="Collapsed" x:Name="CustPhotoProofVerification" Click="CustPhotoProofVerification_Click"/>


                        </Grid>
                        <Grid Margin="0,0,0,10" x:Name="customerProfileGrid">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="Customer Profile Picture" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="CustProfilePictrueError"/>
                            </StackPanel>

                            <Grid Grid.Column="3">
                                <Button Grid.Column="3" Style="{StaticResource Add}" Visibility="{Binding ProfilePicture,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,Converter={StaticResource InverseVisibleImage}}" x:Name="CustProfileAdd" Click="CustProfileAdd_Click"/>
                                <StackPanel Orientation="Horizontal" Grid.Column="3" Visibility="{Binding ProfilePicture,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,Converter={StaticResource VisibleByImage}}">
                                    <Button  Style="{StaticResource Show}" x:Name="CustProfileView" Click="CustProfileView_Click"/>
                                    <Button Grid.Column="3" Style="{StaticResource Edit}" x:Name="CustProfileEdit" Click="CustProfileEdit_Click"/>
                                </StackPanel>
                            </Grid>
                            <Button Grid.Column="4" Style="{StaticResource Verified}" Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}" Visibility="Collapsed" x:Name="CustProfileVerification" Click="CustProfileVerification_Click"/>


                        </Grid>

                        <Grid Margin="0,0,0,10"  x:Name="combineGrid">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="Combine Photo" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="CombinePhotoError"/>
                            </StackPanel>

                            <Grid Grid.Column="3">
                                <Button Grid.Column="3" Style="{StaticResource Add}" Visibility="{Binding CombinePhoto,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,Converter={StaticResource InverseVisibleImage}}" x:Name="CombinePhotoAdd" Click="CombinePhotoAdd_Click"/>
                                <StackPanel Orientation="Horizontal" Grid.Column="3" x:Name="CombinePhotoviewEdit" Visibility="{Binding CombinePhoto,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,Converter={StaticResource VisibleByImage}}">
                                    <Button  Style="{StaticResource Show}" x:Name="CombinePhotoShow" Click="CombinePhotoShow_Click"/>
                                    <Button  Style="{StaticResource Edit}" x:Name="CombinePhotoEdit" Click="CombinePhotoEdit_Click"/>
                                </StackPanel>
                            </Grid>
                            <Button Grid.Column="4" Style="{StaticResource Verified}" Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}" Visibility="Collapsed" x:Name="CombinePhotoVerification" Click="CombinePhotoVerification_Click"/>


                        </Grid>
                        <Grid Grid.Row="1"  Margin="0,0,0,10" x:Name="GuarantorDocumentGrid">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            
                            <Button x:Name="OverAllGuarantorDocumentVerify" Grid.Column="4" Style="{StaticResource Verified}" Visibility="{Binding OverAllGuarantorPhotoVerification,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,Converter={StaticResource Visible}}"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}" Visibility="{Binding OverAllGuarantorPhotoVerification,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,Converter={StaticResource Inverse}}" x:Name="OverAllGuarantorDocumentVerifiedBtn" Click="OverAllGuarantorDocumentVerifiedBtn_Click"/>

                            

                        </Grid>
                        <Grid Margin="0,0,0,10" x:Name="guarantorAddressProofGrid">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="Guarantor Address Proof" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="GAddressProofError"/>
                            </StackPanel>
                            <ComboBox Grid.Column="2"  VerticalAlignment="Center" Width="200" Style="{StaticResource combo}" HorizontalAlignment="Left" ItemsSource="{Binding AddressProofName,Mode=OneWay,UpdateSourceTrigger=PropertyChanged}" Text="{Binding NameofAddressProof,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" x:Name="GNameofAddressProof" SelectionChanged="GNameofAddressProof_SelectionChanged">

                            </ComboBox>
                            <Grid Grid.Column="3" >
                                <Button Grid.Column="3" Style="{StaticResource Add}" Visibility="{Binding AddressProof,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,Converter={StaticResource InverseVisibleImage}}" x:Name="GAddressProofAdd" Click="GAddressProofAdd_Click" IsEnabled="False"/>
                                <StackPanel Orientation="Horizontal" Grid.Column="3" Visibility="{Binding AddressProof,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,Converter={StaticResource VisibleByImage}}">
                                    <Button  Style="{StaticResource Show}" x:Name="GAddressProofShow" Click="GAddressProofShow_Click"/>
                                    <Button Style="{StaticResource Edit}" x:Name="GAddressProofEdit" Click="GAddressProofEdit_Click"/>
                                </StackPanel>
                            </Grid>
                            <Button Grid.Column="4" Style="{StaticResource Verified}" Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}" Visibility="Collapsed" x:Name="GAddressProofVerification" Click="GAddressProofVerification_Click"/>


                        </Grid>
                        <Grid Margin="0,0,0,10" x:Name="guarantorPhotoProofGrid">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="Guarantor Photo Proof" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="GPhotoProofError"/>
                            </StackPanel>
                            <ComboBox Grid.Column="2"  VerticalAlignment="Center" Width="200" Style="{StaticResource combo}" HorizontalAlignment="Left" ItemsSource="{Binding AddressProofName,Mode=OneWay,UpdateSourceTrigger=PropertyChanged}" Text="{Binding NameofPhotoProof,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" x:Name="GNamePhotoProof" SelectionChanged="GNamePhotoProof_SelectionChanged">

                            </ComboBox>
                            <Grid Grid.Column="3" >
                                <Button Grid.Column="3" Style="{StaticResource Add}" Visibility="{Binding PhotoProof,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,Converter={StaticResource InverseVisibleImage}}" x:Name="GPhotoProofAdd" Click="GPhotoProofAdd_Click" IsEnabled="False"/>
                                <StackPanel Orientation="Horizontal" Grid.Column="3" Visibility="{Binding PhotoProof,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,Converter={StaticResource VisibleByImage}}">
                                    <Button  Style="{StaticResource Show}" x:Name="GPhotoProofShow" Click="GPhotoProofShow_Click"/>
                                    <Button Grid.Column="3" Style="{StaticResource Edit}" x:Name="GphotoProof" Click="GphotoProof_Click"/>
                                </StackPanel>
                            </Grid>
                            <Button Grid.Column="4" Style="{StaticResource Verified}" Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}" Visibility="Collapsed" x:Name="GPhotoProofVerification" Click="GPhotoProofVerification_Click"/>


                        </Grid>
                        <Grid Margin="0,0,0,10" x:Name="guarantorprofilegrid">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="Guarantor Profile Photo" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="GProfilePictureError"/>
                            </StackPanel>

                            <Grid Grid.Column="3" >
                                <Button Grid.Column="3" Style="{StaticResource Add}" Visibility="{Binding ProfilePicture,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,Converter={StaticResource InverseVisibleImage}}" x:Name="GprofileAdd" Click="GprofileAdd_Click"/>
                                <StackPanel Orientation="Horizontal" Grid.Column="3" Visibility="{Binding ProfilePicture,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,Converter={StaticResource VisibleByImage}}">
                                    <Button  Style="{StaticResource Show}" x:Name="GProfileShow" Click="GProfileShow_Click"/>
                                    <Button Grid.Column="3" Style="{StaticResource Edit}" x:Name="GprofileEdit" Click="GprofileEdit_Click"/>
                                </StackPanel>
                            </Grid>
                            <Button Grid.Column="4" Style="{StaticResource Verified}" Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}" Visibility="Collapsed" x:Name="GProfileVerification" Click="GProfileVerification_Click"/>


                        </Grid>
                        <Grid Grid.Row="1"  Margin="0,0,0,10" x:Name="NomineeDocumentGrid">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>

                            <Button x:Name="OverAllNomineeDocumentVerify" Grid.Column="4" Style="{StaticResource Verified}" Visibility="{Binding OverAllNomineePhotoVerification,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,Converter={StaticResource Visible}}"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}" Visibility="{Binding OverAllNomineePhotoVerification,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,Converter={StaticResource Inverse}}" x:Name="OverAllNomineeDocumentVerifiedBtn" Click="OverAllNomineeDocumentVerifiedBtn_Click"/>

                        </Grid>
                        <Grid Margin="0,0,0,10" x:Name="nomieeAddressProofGrid">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="Nominee Address Proof" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="NAddressProofError"/>
                            </StackPanel>
                            <ComboBox Grid.Column="2"  VerticalAlignment="Center" Width="200" Style="{StaticResource combo}" HorizontalAlignment="Left" ItemsSource="{Binding AddressProofName,Mode=OneWay,UpdateSourceTrigger=PropertyChanged}" Text="{Binding NameofAddressProof,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" x:Name="NNameofAddressProof" SelectionChanged="NNameofAddressProof_SelectionChanged">

                            </ComboBox>
                            <Grid Grid.Column="3" >
                                <Button Grid.Column="3" Style="{StaticResource Add}" Visibility="{Binding AddressProof,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,Converter={StaticResource InverseVisibleImage}}" x:Name="NAddressProofAdd" Click="NAddressProofAdd_Click" IsEnabled="False"/>
                                <StackPanel Orientation="Horizontal" Grid.Column="3" Visibility="{Binding AddressProof,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,Converter={StaticResource VisibleByImage}}">
                                    <Button  Style="{StaticResource Show}" x:Name="NAddressProofShow" Click="NAddressProofShow_Click"/>
                                    <Button Grid.Column="3" Style="{StaticResource Edit}" x:Name="NAddressProofEdit" Click="NAddressProofEdit_Click"/>
                                </StackPanel>
                            </Grid>
                            <Button Grid.Column="4" Style="{StaticResource Verified}" Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}" Visibility="Collapsed" x:Name="NAddressProofVerification" Click="NAddressProofVerification_Click"/>


                        </Grid>
                        <Grid Margin="0,0,0,10" x:Name="nomineePhotoproofGrid">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="Nominee Photo Proof" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="NPhotoProofError"/>
                            </StackPanel>
                            <ComboBox Grid.Column="2"  VerticalAlignment="Center" Width="200" Style="{StaticResource combo}" HorizontalAlignment="Left" ItemsSource="{Binding AddressProofName,Mode=OneWay,UpdateSourceTrigger=PropertyChanged}" Text="{Binding NameofPhotoProof,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" x:Name="NNameofPhotoProof" SelectionChanged="NNameofPhotoProof_SelectionChanged">

                            </ComboBox>
                            <Grid Grid.Column="3" >
                                <Button Grid.Column="3" Style="{StaticResource Add}" Visibility="{Binding PhotoProof,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,Converter={StaticResource InverseVisibleImage}}" x:Name="NPhotoProofAdd" Click="NPhotoProofAdd_Click" IsEnabled="False"/>
                                <StackPanel Orientation="Horizontal" Grid.Column="3" Visibility="{Binding PhotoProof,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,Converter={StaticResource VisibleByImage}}">
                                    <Button  Style="{StaticResource Show}" x:Name="NPhotoProofShow" Click="NPhotoProofShow_Click"/>
                                    <Button Grid.Column="3" Style="{StaticResource Edit}" x:Name="NPhotoProofEdit" Click="NPhotoProofEdit_Click"/>
                                </StackPanel>
                            </Grid>
                            <Button Grid.Column="4" Style="{StaticResource Verified}" Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}" Visibility="Collapsed" x:Name="NPhotoProofVerification" Click="NPhotoProofVerification_Click"/>


                        </Grid>
                        <Grid Margin="0,0,0,10" x:Name="nomineeprofileGrid">
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="130"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="Nominee Profile Photo" Style="{StaticResource Regular}"/>
                                <TextBlock  Style="{StaticResource ErrorStar}" Visibility="Collapsed" x:Name="NProfileError"/>
                            </StackPanel>

                            <Grid Grid.Column="3" >
                                <Button Grid.Column="3" Style="{StaticResource Add}" Visibility="{Binding ProfilePicture,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,Converter={StaticResource InverseVisibleImage}}"  x:Name="NProfileAdd" Click="NProfileAdd_Click"/>
                                <StackPanel Orientation="Horizontal" Grid.Column="3" Visibility="{Binding ProfilePicture,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,Converter={StaticResource VisibleByImage}}">
                                    <Button  Style="{StaticResource Show}" x:Name="NProfileView" Click="NProfileView_Click" />
                                    <Button Grid.Column="3" Style="{StaticResource Edit}" x:Name="NProfileEdit" Click="NProfileEdit_Click"/>
                                </StackPanel>
                            </Grid>
                            <Button Grid.Column="4" Style="{StaticResource Verified}" Visibility="Collapsed"/>
                            <Button Grid.Column="4" Style="{StaticResource Verify}" Visibility="Collapsed" x:Name="NProfileVerification" Click="NProfileVerification_Click"/>


                        </Grid>
                    </StackPanel>
                </Border>
            </StackPanel>

        </ScrollViewer>

        <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Center">
            <Button Style="{StaticResource CancelBtn}" x:Name="CancelVerify" Click="CancelVerify_Click" Content="Back"/>
            <Button Grid.Row="2" Style="{StaticResource PrimaryBtn}" Content="Save" x:Name="VerifyNewCustomer" Click="VerifyNewCustomer_Click" Margin="30,0,0,0"/>
        </StackPanel>



        <!--Capture Panel-->
        <Grid Grid.Row="0" Grid.RowSpan="2" Grid.Column="0" Grid.ColumnSpan="7" Width="1366" Height="630" x:Name="CaptureImage" Visibility="Collapsed" Panel.ZIndex="1" >
            <Border Background="#e8fff2" BorderBrush="Black" BorderThickness="2" Width="800" Height="450">
                <Grid Height="450" Width="800" x:Name="capturepanel" >
                    <Grid Height="450" Width="800">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="40*"/>
                            <RowDefinition Height="370*"/>
                            <RowDefinition Height="40*"/>
                            <RowDefinition Height="40*"/>
                        </Grid.RowDefinitions>
                        <Border Grid.Column="0" Grid.ColumnSpan="3" BorderBrush="Black" BorderThickness="0,0,0,1.5" Height="40" VerticalAlignment="Top"  >
                            <TextBlock  x:Name="PhotoProofNametxt" Style="{StaticResource SubTitle}" HorizontalAlignment="Right" Margin="0,0,10,0"/>
                        </Border>
                        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
                            <Button Style="{StaticResource CancelBtn}" Content="Cancel" Name="ImgCancelbtn" Click="ImgCancelbtn_Click" Margin="0,0,10,0"/>
                            <Button Style="{StaticResource PrimaryBtn}" Name="ImageSavebtn" Click="ImageSavebtn_Click" Content="Save" FontSize="16"></Button>
                        </StackPanel>
                        <Border BorderBrush="Black" BorderThickness="0,1.5,0,0" Grid.Row="3">
                            <StackPanel Orientation="Horizontal" x:Name="CaptureImageStatus">
                                <Image Width="25" Height="25" Margin="5,-5,0,0" Source="{Binding MessageType,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,Converter={StaticResource ImageConvert}}"/>
                                <TextBlock HorizontalAlignment="Left" Margin="10,0,0,0" FontWeight="SemiBold" Text="{Binding StatusMessage,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Foreground="{Binding MessageType,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,Converter={StaticResource StatusColor}}" Style="{StaticResource Regular}"/>
                            </StackPanel>
                        </Border>
                        <Frame x:Name="Captureframe" Grid.Row="1">

                        </Frame>
                    </Grid>
                </Grid>
            </Border>

        </Grid>

        <!--View Image-->
        <Popup x:Name="ViewImagePopup" Placement="Center" PlacementTarget="{Binding ElementName=PageGrid}">
            <Border BorderBrush="Black" BorderThickness="2" Height="300" Width="300">
                <Grid Background="#e8fff2" Height="300" Width="300">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="30"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="50"/>
                        <RowDefinition Height="10 "/>
                    </Grid.RowDefinitions>
                    <Border BorderBrush="Black" BorderThickness="0,0,0,2" Grid.Row="0">
                        <TextBlock x:Name="ImageTitle" Text="Profile Picture" Style="{StaticResource SubTitle}" HorizontalAlignment="Right" Margin="0,0,10,0"/>
                    </Border>
                    <Border Width="250" Height="150" BorderThickness="2" BorderBrush="BlueViolet" Grid.Row="1">
                        <Image x:Name="viewImage" Width="250" Height="150"/>
                    </Border>
                    <Button Style="{StaticResource SecondaryBtn}" Grid.Row="2" Content="OK" Name="ProfileImageOk" Click="ProfileImageOk_Click"/>
                </Grid>
            </Border>
        </Popup>

    </Grid>
    
</Page>
